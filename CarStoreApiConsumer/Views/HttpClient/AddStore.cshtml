@model Store
@{
    ViewBag.Title = "Add new car store";
}

<h2 class="text-center">@ViewBag.Title</h2>

<div>
    <form method="post">
        <div class="form-group">
            <label for="City">City</label>
            <input for="City" type="text" class="form-control" name="City">
        </div>
        <div class="form-group">
            <label for="Description">Description</label>
            <input for="Description" type="text" class="form-control" name="Description">
        </div>
        <div class="form-group">
            <div id="newCar"></div>
            <label id="carList">Click to add car</label>
        </div>
        <button type="submit" class="btn btn-primary">Add</button>
    </form>
</div>

@section scripts{
    
        <script src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.2.1.min.js"></script>
    <script>
        $(document).ready(function () {
                $("#carList").click(function () {
                    var stores = @Model.CarList;
                    $.each(stores, function (key, value) {

                    })
                })
            });
    </script>
}